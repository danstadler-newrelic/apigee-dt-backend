html
  head
    title this is my title
  body
    h1 Web Socket Example
    p open console to see connection.
    script.
      var ws = new WebSocket('ws://#{domainAndPort}');
      ws.onopen = function () {
        console.log('websocket is connected ...')
        ws.send('connected')
      }
        ws.onmessage = function (ev) {
        console.log(ev);
      }
